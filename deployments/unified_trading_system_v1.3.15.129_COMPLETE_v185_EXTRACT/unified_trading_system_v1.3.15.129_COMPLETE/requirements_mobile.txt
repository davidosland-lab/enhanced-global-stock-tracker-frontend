# Mobile Remote Access Dependencies
# Install with: pip install -r requirements_mobile.txt

# Core dependencies (already in main requirements.txt)
dash>=2.0.0
plotly>=5.0.0
pandas>=1.3.0
numpy>=1.21.0

# Mobile access specific
qrcode[pil]>=7.3.1          # QR code generation with image support
Pillow>=9.0.0               # Image processing for QR codes
requests>=2.27.0            # HTTP client for ngrok API

# Security
secrets                     # Secure random token generation (built-in Python 3.6+)
hashlib                     # Password hashing (built-in)

# Note: ngrok must be installed separately from https://ngrok.com/download
