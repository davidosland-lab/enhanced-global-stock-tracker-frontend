# FinBERT Ultimate - WORKING Requirements
# Install in this exact order for best results

# Step 1: Core dependencies
numpy==1.26.4
pandas>=2.0.0

# Step 2: PyTorch (install BEFORE transformers)
# Manual install recommended:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Step 3: Transformers (AFTER PyTorch)
transformers==4.36.0

# Step 4: Other dependencies
yfinance>=0.2.28
flask>=3.0.0
flask-cors>=4.0.0
scikit-learn>=1.3.0
ta>=0.11.0
feedparser>=6.0.10
requests>=2.31.0
tqdm>=4.65.0