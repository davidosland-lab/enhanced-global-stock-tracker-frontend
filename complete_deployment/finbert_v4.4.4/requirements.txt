# FinBERT v4.4 - Phase 1 Requirements
# Updated: 2025-11-09 - Added curl_cffi requirement for yfinance 0.2.x+

# Core Dependencies (Required)
flask>=2.3.0
flask-cors>=4.0.0

# yfinance - Yahoo Finance data PRIMARY source (MUST be 0.2.x+ for curl_cffi support)
# CRITICAL: Do NOT pass custom session to yf.Ticker() - breaks curl_cffi
yfinance>=0.2.66

# yahooquery - Yahoo Finance FALLBACK source (automatic failover when yfinance blocked)
# Provides identical OHLCV data via different Yahoo Finance API endpoints
yahooquery>=2.3.7

# curl_cffi - Required by yfinance 0.2.x+ (auto-installed with yfinance)
curl_cffi>=0.13.0

pandas>=1.5.0
numpy>=1.24.0
requests>=2.31.0

# Technical Analysis (Highly Recommended)
ta>=0.11.0

# LSTM Training (Optional - for neural network predictions)
tensorflow>=2.13.0
keras>=2.13.0
scikit-learn>=1.3.0

# Sentiment Analysis (Optional - for FinBERT sentiment)
transformers>=4.30.0
torch>=2.0.0

# Job Scheduling (Optional - for automated predictions)
APScheduler>=3.10.0

# Additional Utilities
python-dateutil>=2.8.2
pytz>=2023.3

# Web Scraping & Parsing
beautifulsoup4>=4.13.0
lxml>=6.0.0
feedparser==6.0.10
